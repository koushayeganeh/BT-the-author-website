import "./HeroContainer.css";
import HeroSection from "./HeroSection";
const HeroContainer = () => {
  return (
    <section className="section17" id="hero-section">
      <HeroSection />
    </section>
  );
};

export default HeroContainer;
